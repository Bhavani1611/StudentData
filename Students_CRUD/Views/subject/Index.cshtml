@model IEnumerable<Students_CRUD.Models.mst_subject>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.subject_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.subject_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Subject_key }) |
            @Html.ActionLink("Details", "Details", new { id=item.Subject_key }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Subject_key })
        </td>
    </tr>
}

</table>
